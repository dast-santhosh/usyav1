[workspace]
resolver = "2"
members = [
    "boot",
    "kernel",
    "hal",
    "drivers/storage",
    "drivers/usb",
    "drivers/network",
    "drivers/gpu",
    "posix",
    "security",
    "xtask"
, "drivers/input"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
